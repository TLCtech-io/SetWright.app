node_modules/
dist/
*.log
.DS_Store
archive/
.claude/settings.local.json
.vercel/

# Local env files hold real secrets — never commit. Cover EVERY variant (.env, .env.local,
# .env.production, …) and re-allow the committed template.
.env
.env.*
!.env.example

# Playwright E2E artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Test DB Seed with Auth User UUID
supabase/hosted-test-seed.sql

migration/
